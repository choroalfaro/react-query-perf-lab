const containerStyle = {
  display: 'flex',
  height: '100vh',
  alignItems: 'center',
  justifyContent: 'center',
};

function App() {
  return (
    <div style={containerStyle}>
      <div>React Query Perf Lab 🚀</div>
    </div>
  );
}

export default App;
